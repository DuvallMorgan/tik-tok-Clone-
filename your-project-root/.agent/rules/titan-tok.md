# 🚀 ANTIGRAVITY MISSION: TITAN-TOK TAKEOVER

## 🎯 ARCHITECTURE CONSTRAINTS
- **Face:** Next.js (App Router) using Tailwind CSS.
- **Brain:** Supabase (PostgreSQL + Realtime).
- **Vault:** Mux (Video Infrastructure).
- **The Threshold:** All interactions must be < 400ms (Doherty Threshold).

## 🛠️ AGENT COMMANDS
- **Docling Scan:** When processing raw TikTok metadata, always route through the `docling` parser.
- **Submarine Mode:** Background tasks must be implemented as Python/LangChain workers on GCP.
- **Goku Speed:** Prioritize CSS hardware acceleration over JS-heavy animations.

## 🕵️ STEALTH & PRIVACY
- All anonymous browsing must be handled by the **Creep Mode** logic (anonymize UserIDs).
- Ensure 24/7 "Submarine" persistence for live-lead triage.

## 📦 DEPENDENCIES
- Use `agora-rtc-sdk-ng` for 5G Live Streaming.
- Use `lucide-react` for iconography (Japan Y2K style).
