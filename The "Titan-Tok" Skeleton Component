export const VideoSkeleton = () => {
  return (
    <div className="h-screen w-full bg-black relative flex flex-col justify-end p-6 overflow-hidden">
      {/* 3D "Background" energy Pulse */}
      <div className="absolute inset-0 skeleton-ssj opacity-20" />

      {/* Profile & Info Skeleton (The "Brain" Placeholder) */}
      <div className="flex flex-col gap-4 mb-12 z-10">
        <div className="flex items-center gap-3">
          <div className="w-12 h-12 rounded-full skeleton-ssj skeleton-pulse-aura" />
          <div className="h-4 w-32 skeleton-ssj" />
        </div>
        <div className="h-3 w-3/4 skeleton-ssj" />
        <div className="h-3 w-1/2 skeleton-ssj" />
      </div>

      {/* Right Side Buttons (Share, Like, Creep Mode) */}
      <div className="absolute right-4 bottom-32 flex flex-col gap-8 z-10">
        {[1, 2, 3, 4].map((i) => (
          <div key={i} className="flex flex-col items-center gap-1">
            <div className="w-12 h-12 rounded-full skeleton-ssj skeleton-pulse-aura" />
            <div className="h-2 w-6 skeleton-ssj" />
          </div>
        ))}
      </div>
    </div>
  );
};
