# 🧠 MASTER SYSTEM: STATE-AWARE PROMPTING

## 🛠️ THE CLI MEMORY COMMANDS
We use `gemini-cli` with `--checkpointing` to ensure the AI never forgets the "Submarine" logic.

### 1. The "State Awareness" Anchor
**Command:** `/memory add "Project Architecture: NextJS (Face), Supabase (Brain), Mux (Vault). Threshold: <400ms."`
* **Why:** Prevents the AI from suggesting slow, bloated libraries.

### 2. The "Submarine" Stealth Logic
**Command:** `/memory add "All background workers run in Python/LangChain on GCP Asia-Southeast1."`
* **Why:** Ensures all code snippets follow the 24/7 "Submarine" deployment style.

## 👾 MONSTER-STYLE CODE SNIPPETS (The Takeover)

### The "Goku" Interceptor (Middleware)
This CSS/JS hybrid ensures the UI "shivers" with power before the data even arrives.

```css
/* Japan Y2K Tech Takeover Pulse */
.data-tunnel-shimmer {
  background: repeating-linear-gradient(
    45deg,
    #000,
    #000 10px,
    #00f2ff1a 10px,
    #00f2ff1a 20px
  );
  animation: takeover-scan 0.5s infinite linear;
}

@keyframes takeover-scan {
  from { background-position: 0 0; }
  to { background-position: 40px 0; }
}
